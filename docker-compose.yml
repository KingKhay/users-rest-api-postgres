version: '3.8'

services:
  users-db:
    image: postgres
    container_name: user-db
    restart: always
    ports:
      - "5436:5432"
    environment:
      POSTGRES_DB: ${dbUrl}
      POSTGRES_USER: ${dbUser}
      POSTGRES_PASSWORD: ${dbPassword}
